import UIKit

// BREAKPOINTS
// Al apretar sobre la linea de código (antes del número) se pone en azul (Se crea un break point
// Al querer correr la app no aparece nada ya que se creo un punto de frenado
// Podemos ver esto en el debug area recordar que la podemos activar en view >> debug area >> activate console
// El simbolo de play en el debug area hace que se corra la aplicacion desde el breakpoint sucesivamente en caso de que haya varios
// Para desactivar el breakpoint y utilizarlo talvez más adelatne pulsamos sobre el (lo que este en azul) y se volverá un poco más transparente, al correr la app no lo tendrá en cuenta pero si queremos eliminarlo definitibamente debemos hacer segundo click delete Breakpoint

// En caso de tener varios breakpoints sobre el mismo label, para ver los cambios podemos hacerlo en el debug area, yendo a sef >> nombre de la etiqueta label >> content >> string , ahí podremos ver como va cambiando cuando le damos play para que salte de breakpoint en breakpoint
// En el debug area al lado del boton de play tenemos el boton step over lo cual nos permite ir paso a paso linea a linea despues del punto de breakpoint sin tener estos luego del primero
